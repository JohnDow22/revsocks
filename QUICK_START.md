Настроить конфиг агента
config/revsocks.yaml

Сборка :
./build.sh server && ./build.sh stealth

Запуск сервера (TLS включён по умолчанию):
./revsocks-server -listen :443 -socks :10443 -pass SFOpkm3rffAds90SF3ghSD -admin-api -admin-port :8081 -ws

Запуск консоли
Настроить виртуальное окружение
Затем
cd tools/console
python3 main.py

Работа с агентами
-Отправить спать на минуту
agent sleep notebook 1m

-Разбудить
agent wake notebook






# Quick Start - RevSocks

Этот гайд поможет быстро запустить сервер и агент RevSocks. Для детального изучения фич см. [Features Index](docs/04_Features/FEATURES_INDEX.md).

## 🚀 Быстрый старт

1. **Подготовка:**
```bash
./build.sh server
./build.sh stealth
```

2. **Запуск сервера (TLS по умолчанию):**
```bash
./revsocks-server -listen :10443 -socks :1080 -pass YOUR_PASSWORD -admin-api
```

> ⚠️ **TLS теперь включён по умолчанию!** Для локальной отладки без TLS используйте `-insecure`.

3. **Запуск агента:**
```bash
./revsocks-agent_v3
```

---

## 📋 Основные режимы

RevSocks поддерживает несколько режимов работы для обхода сетевых ограничений.

### 1. TCP + TLS (По умолчанию)
Самый быстрый режим с шифрованием. **TLS теперь включён автоматически.**
- **Инструкция:** [STEALTH_BUILD.md](docs/04_Features/STEALTH_BUILD.md)
- **Failover:** [MULTI_SERVER_FAILOVER.md](docs/04_Features/MULTI_SERVER_FAILOVER.md)

> 💡 Для отладки без TLS: `./revsocks-server -insecure ...` (выводит warning)

### 2. WSS (Websocket over TLS)
Для обхода DPI и фаерволов, трафик выглядит как обычный HTTPS.
- **Настройка:** В `config/revsocks.yaml` установите `use_websocket: true` и `address: "wss://..."`.

### 3. Beacon Mode (Маякование)
Режим с периодическими проверками вместо постоянного соединения.
- **Подробности:** [BEACON_MODE.md](docs/04_Features/BEACON_MODE.md)

---

## 🛠 Управление агентами

Для управления парком агентов используйте **Admin Console**.

1. Перейдите в `tools/console`.
2. Настройте `REVSOCKS_TOKEN`.
3. Запустите `python3 main.py`.

**Основные команды:**
- `agents list` — список всех агентов;
- `agent sleep <id> <time>` — отправить агента в спячку;
- `agent wake <id>` — разбудить агента (перевести в TUNNEL режим).

Подробнее см. [EXTENDED_AGENT_INFO_UI.md](docs/04_Features/EXTENDED_AGENT_INFO_UI.md).

---

## 🔍 Проверка и Troubleshooting

### Проверка связи
```bash
curl -x socks5://127.0.0.1:1080 https://ifconfig.me
```

### Частые проблемы
- **Connection refused:** Проверьте firewall на сервере.
- **TLS handshake error:** Убедитесь, что пароли совпадают, или используйте `verify: false` для самоподписанных сертификатов.

---

## 📚 Дополнительная документация

- [Features Index](docs/04_Features/FEATURES_INDEX.md) — все возможности;
- [Bugfixes Index](docs/05_Bugfixes/BUGFIXES_INDEX.md) — история исправлений;
- [CHANGELOG.md](docs/CHANGELOG.md) — история версий.
