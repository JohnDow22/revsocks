# E2E Testing Framework для RevSocks

## 📋 Описание

Полнофункциональная система End-to-End тестирования для RevSocks. Тестирует собранные бинарники как "Black Box" - проверяет CLI аргументы, сетевое взаимодействие и проксирование трафика в реальных условиях.

## 🏗 Архитектура

```
tests/e2e/
├── framework.go        # Глобальный контекст (TestContext)
├── builder.go          # Компиляция бинарника во временную директорию
├── process.go          # Управление процессами (Start/Stop/WaitForLog)
├── target.go           # Echo-сервер для проверки проксирования
├── traffic.go          # SOCKS5 клиент для тестирования
├── main_test.go        # Точка входа (TestMain)
├── scenarios_test.go   # Базовые тесты (TCP)
└── websocket_test.go   # WebSocket тесты (WS/WSS)
```

## 🚀 Запуск

### Запуск всех тестов
```bash
cd /path/to/revsocks
go test -v ./tests/e2e/... -timeout 30s
```

### Запуск конкретного теста
```bash
go test -v ./tests/e2e/... -run TestE2E_Basic
```

### Запуск с параллелизмом
```bash
# По умолчанию тесты запускаются последовательно (-p 1)
# Параллельный запуск возможен благодаря GetFreePort()
go test -v ./tests/e2e/... -parallel 4
```

## 📝 Реализованные сценарии

### ✅ TestE2E_Basic
Базовый сценарий проверки функциональности:
1. Запуск Echo сервера (target)
2. Запуск RevSocks сервера с SOCKS listener
3. Запуск RevSocks клиента (агента)
4. Проксирование TCP трафика через туннель
5. Проверка целостности данных (echo response)

**Проверяет:**
- Компиляцию бинарника
- Парсинг CLI аргументов
- Установку Yamux туннеля
- SOCKS5 проксирование
- Передачу данных через туннель

### ✅ TestE2E_Reconnect
Проверка переподключения клиента при разрыве:
1. Запустить server + client
2. Убить клиента (SIGKILL)
3. Перезапустить клиента
4. Проверить восстановление туннеля

**Проверяет:**
- Переподключение агента после разрыва
- Переиспользование порта при reconnect
- Восстановление работоспособности туннеля

### ✅ TestE2E_MultipleClients
Проверка работы с несколькими клиентами:
1. Запустить server
2. Запустить 2+ клиентов с разными agent_id
3. Проверить проксирование через каждого

**Проверяет:**
- SessionManager логику замены сессий
- Кеширование портов per agentID
- Корректное переключение между клиентами

### ✅ TestE2E_CurlRealProxy
Реальная проверка SOCKS5 прокси через `curl`:
1. Запуск server + agent
2. Проверка через `curl --socks5` запроса к google.com
3. Валидация HTTP ответа

**Проверяет:**
- Совместимость с реальными SOCKS5 клиентами
- HTTPS проксирование через туннель
- HTTP проксирование через туннель
- Работу без моков и тестовых серверов

**Запуск:**
```bash
go test -v ./tests/e2e/... -run TestE2E_CurlRealProxy
```

### ✅ TestE2E_WebSocket_Basic
Базовое WebSocket соединение (ws://):
1. Запуск сервера в WebSocket режиме (-ws)
2. Агент подключается через ws://
3. Агент получает команду от сервера (v3 протокол)
4. Проксирование трафика

**Проверяет:**
- WebSocket handshake
- Протокол v3 (CMD TUNNEL/SLEEP)
- Yamux туннель через WebSocket

### ✅ TestE2E_WebSocket_TLS
WebSocket с TLS (wss://):
1. Запуск сервера с флагами -ws -tls
2. Агент подключается через wss://
3. Проксирование через зашифрованный туннель

**Проверяет:**
- TLS генерация сертификата
- WSS handshake
- Шифрованное проксирование

### ✅ TestE2E_WebSocket_SleepWake
SLEEP/WAKE через WebSocket:
1. Агент подключается через WS и получает TUNNEL
2. Admin API переводит в SLEEP режим
3. Агент уходит в сон
4. Admin API переводит в TUNNEL (wake)
5. Проверка работоспособности прокси

**Проверяет:**
- v3 протокол для WebSocket
- SLEEP/WAKE transitions
- Интеграция с Admin API

### ✅ TestE2E_WebSocket_V3Protocol
Проверка что WebSocket использует v3 протокол:
1. Агент получает CMD TUNNEL от сервера
2. Сервер регистрирует агента в AgentManager
3. Туннель работает

**Проверяет:**
- Корректность v3 handshake для WebSocket
- Регистрация в AgentManager

## 🔧 Компоненты

### Builder (builder.go)
- Компилирует **один** бинарник RevSocks во временную директорию
- Использует абсолютный путь к корню проекта
- Автоматически добавляет `.exe` на Windows
- Возвращает cleanup функцию для удаления временных файлов

### Process Manager (process.go)
- Запуск процессов с произвольными аргументами
- Захват stdout/stderr
- Ожидание появления паттерна в логах (WaitForLog)
- Graceful/Force stop
- Thread-safe доступ к буферам

### Echo Server (target.go)
- TCP сервер на случайном порту (Listen ":0")
- Возвращает полученные данные обратно
- Используется как endpoint для проверки проксирования

### Traffic Generator (traffic.go)
- SOCKS5 клиент через `golang.org/x/net/proxy`
- Отправляет тестовые данные через прокси
- Проверяет целостность echo ответа

### Curl Tests (curl_test.go)
- Реальные проверки через системный `curl`
- HTTPS/HTTP запросы через SOCKS5
- Проверка совместимости с внешними клиентами
- Автоматический skip если curl не установлен

## 🎯 Принципы

1. **Black Box Testing** - тесты не знают о внутренней реализации
2. **Isolation** - каждый тест использует свободные порты (GetFreePort)
3. **Real Binary** - компилируется текущий код, а не старый бинарник
4. **Deterministic** - тесты независимы и повторяемы
5. **Fast Feedback** - параллельная компиляция в TestMain

## ⚠️ Ограничения

1. **Порты** - GetFreePort() резервирует порт и сразу закрывает listener. Между закрытием и использованием портом может быть race condition. На практике конфликты редки благодаря sleep 100ms.

2. **Логи** - WaitForLog делает polling каждые 100ms. Для очень быстрых событий может быть race.

3. **Cleanup** - При падении теста процессы могут остаться. Используйте `defer` для Stop().

## 🐛 Отладка

### Тест падает с "timeout waiting for log"
```bash
# Увеличьте таймаут или проверьте что процесс запустился
t.Logf("Output: %s", process.GetOutput())
```

### Тест падает с "connection refused"
```bash
# Проверьте что порты не конфликтуют
netstat -tulpn | grep <port>

# Увеличьте sleep после Start
time.Sleep(1 * time.Second)
```

### Процессы остались после теста
```bash
# Найти и убить вручную
ps aux | grep revsocks-test
kill -9 <PID>
```

## 📊 Покрытие

E2E тесты проверяют:
- ✅ Компиляцию и запуск
- ✅ Парсинг аргументов (-listen, -connect, -pass, -socks)
- ✅ Handshake v3 протокол
- ✅ Yamux туннель (client → server)
- ✅ SOCKS5 проксирование (через golang proxy)
- ✅ SOCKS5 проксирование (через реальный curl)
- ✅ Передачу данных
- ✅ Reconnect логику
- ✅ Multiple clients
- ✅ TLS режим (TCP)
- ✅ Beacon mode (SLEEP/TUNNEL cycles)
- ✅ WebSocket режим (WS)
- ✅ WebSocket TLS режим (WSS)
- ✅ WebSocket v3 протокол (CMD TUNNEL/SLEEP)
- ✅ WebSocket SLEEP/WAKE transitions
- ⏳ Stealth mode (TODO)

## 🔗 Связанные документы

- План: `/plans/2026-01-09_RevSocks_E2E_Testing/`
- Bugfix план: `/plans/2026-01-09_RevSocks_Bugfix/`
- Основной README: `../../README.md`
