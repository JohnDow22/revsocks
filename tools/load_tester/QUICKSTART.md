# 🚀 Quick Start: Security Testing

## Установка

```bash
cd Linux/MyCustomProjects/RevSocks_my/revsocks/tools/load_tester/

# Установить зависимости
pip3 install requests websocket-client
```

## Запуск сервера (для тестирования)

```bash
cd ../../

# Запуск тестового сервера
./revsocks-server \
  -listen :10443 \
  -socks 192.168.1.108:60281 \
  -pass SFOpkm3rffAds90SF3ghSD \
  -tls \
  -admin-api \
  -admin-port :8081 \
  -ws
```

## 🎯 Быстрый тест (автоматический)

```bash
# Запускает ВСЕ проверки последовательно
./run_security_tests.sh

# С кастомными параметрами
SERVER_HOST=10.0.0.5 \
SERVER_PORT=10443 \
SERVER_PASS="your_password" \
./run_security_tests.sh
```

**Результат:** Автоматически проверит все аспекты безопасности и выдаст итоговый отчет.

## 🔧 Ручной режим

### 1. Все тесты сразу

```bash
python3 server_stress_test.py \
  --host 192.168.1.108 \
  --port 10443 \
  --admin-port 8081 \
  --password "SFOpkm3rffAds90SF3ghSD" \
  --tls \
  --mode all
```

### 2. Конкретная проверка

```bash
# Флуд авторизацией (проверка утечек памяти)
python3 server_stress_test.py --mode auth_flood --workers 20

# Неверные пароли (проверка защиты от брутфорса)
python3 server_stress_test.py --mode invalid_auth --workers 30

# Быстрые переподключения (SessionManager)
python3 server_stress_test.py --mode rapid_reconnect --iterations 200

# Атаки на Admin API
python3 server_stress_test.py --mode admin_api
```

## ✅ Проверка после теста

```bash
# 1. Сервер жив?
curl http://192.168.1.108:8081/api/sessions

# 2. Память в норме?
ps aux | grep revsocks-server

# 3. Соединения закрылись?
netstat -an | grep 10443 | wc -l

# 4. Агент подключается?
./revsocks-agent_v3 -connect wss://192.168.1.108:10443 -pass SFOpkm3rffAds90SF3ghSD
```

## 🐛 Если нашли баг

1. **Сохраните логи сервера**
2. **Запишите какой тест упал**
3. **Проверьте есть ли panic/crash**
4. **Попробуйте воспроизвести**

Пример воспроизведения:
```bash
# Изолированная проверка
python3 server_stress_test.py \
  --mode rapid_reconnect \
  --workers 1 \
  --iterations 10
  
# Постепенно увеличивайте нагрузку
```

## 📊 Ожидаемые результаты

**Хороший сервер:**
- ✓ Success rate > 95%
- ✓ Нет паники в логах
- ✓ Память стабильна
- ✓ Admin API отвечает
- ✓ Агент подключается после теста

**Плохой сервер:**
- ✗ Success rate < 80%
- ✗ Панически/ошибки в логах
- ✗ Рост памяти
- ✗ Накопление соединений
- ✗ Сервер не отвечает

## 💡 Советы

1. **Начните с малой нагрузки** (`--workers 5 --iterations 20`)
2. **Увеличивайте постепенно** (5 → 10 → 20 → 50)
3. **Мониторьте в реальном времени** (`tail -f logs.txt`)
4. **Тестируйте в безопасном окружении** (не на проде!)
5. **Делайте паузы между тестами** (дайте серверу "остыть")

## 🚨 Красные флаги

**Немедленно остановите деплой если:**
- Сервер падает во время теста
- Панический/critical ошибки в логах
- Память растет и не освобождается
- Соединения не закрываются (утечка дескрипторов)
- Admin API не отвечает после теста
- Success rate < 70%

---

**Полная документация:** [README.md](./README.md)
