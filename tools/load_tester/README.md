# RevSocks Load Testing Tools

Инструменты для нагрузочного и стресс-тестирования RevSocks прокси.

## 📁 Файлы

### 1. `stress_test.py` - Клиентский нагрузочный тест

Тестирует SOCKS5 прокси через множественные HTTP запросы.

**Назначение:** Проверка пропускной способности и стабильности прокси под нагрузкой.

**Установка зависимостей:**
```bash
pip3 install requests[socks]
```

**Использование:**
```bash
# Базовый запуск (10 потоков, 1 сек интервал)
python3 stress_test.py

# Агрессивный тест (50 потоков, без паузы)
python3 stress_test.py --threads 50 --interval 0.1

# Кастомный прокси
python3 stress_test.py \
  --proxy-host 10.0.0.5 \
  --proxy-port 1080 \
  --proxy-user admin \
  --proxy-pass secret123

# Ограниченный тест (100 итераций)
python3 stress_test.py --max-iterations 100
```

### 2. `server_stress_test.py` - Серверный стресс-тест

**НОВЫЙ** - Агрессивное тестирование серверной части RevSocks.

**Назначение:** Поиск уязвимостей, проверка устойчивости к атакам перед продакшеном.

**⚠️ ВНИМАНИЕ:** Может уронить/деградировать сервер! Только для тестовых окружений!

**Установка зависимостей:**
```bash
pip3 install requests websocket-client
```

**Типы атак:**

| Режим | Описание | Проверяет |
|-------|----------|-----------|
| `auth_flood` | Флуд валидными подключениями | Утечки памяти/дескрипторов |
| `invalid_auth` | Флуд неверными паролями | Защита от брутфорса |
| `malformed_data` | Отправка мусорных данных | Парсинг и обработку ошибок |
| `slowloris` | Медленные клиенты | Таймауты и защиту от slowloris |
| `rapid_reconnect` | Быстрые переподключения | SessionManager и cleanup |
| `admin_api` | Атаки на Admin API | SQL injection, path traversal, XSS |
| `all` | Все тесты последовательно | Комплексная проверка |

**Использование:**

```bash
# Все тесты (рекомендуется для первого запуска)
python3 server_stress_test.py \
  --host 192.168.1.108 \
  --port 10443 \
  --admin-port 8081 \
  --password "SFOpkm3rffAds90SF3ghSD" \
  --tls \
  --mode all

# Только проверка авторизации
python3 server_stress_test.py \
  --mode auth_flood \
  --workers 20 \
  --iterations 100

# Атака на Admin API
python3 server_stress_test.py \
  --mode admin_api \
  --workers 5

# Быстрые переподключения (проверка SessionManager)
python3 server_stress_test.py \
  --mode rapid_reconnect \
  --workers 10 \
  --iterations 200
```

**Параметры:**
- `--host` - IP адрес сервера (по умолчанию: 192.168.1.108)
- `--port` - Порт сервера (по умолчанию: 10443)
- `--admin-port` - Порт Admin API (по умолчанию: 8081)
- `--password` - Пароль сервера
- `--tls` - Использовать TLS/WSS
- `--mode` - Режим тестирования (см. таблицу выше)
- `--workers` - Количество параллельных потоков (по умолчанию: 10)
- `--iterations` - Количество итераций (по умолчанию: 50)

## 🎯 Workflow тестирования перед продакшеном

### Этап 1: Базовая проверка
```bash
# Запускаем сервер
./revsocks-server -listen :10443 -socks 127.0.0.1:1080 -pass test123 -tls -admin-api -admin-port :8081 -ws

# Проверяем базовую функциональность (1-2 минуты)
python3 server_stress_test.py --mode auth_flood --iterations 50
```

**Проверить после теста:**
- [ ] Сервер не упал
- [ ] Нет паник в логах
- [ ] Admin API отвечает (`curl http://127.0.0.1:8081/api/sessions`)
- [ ] Валидный агент может подключиться

### Этап 2: Проверка защиты
```bash
# Неверные пароли
python3 server_stress_test.py --mode invalid_auth --workers 20 --iterations 100

# Некорректные данные
python3 server_stress_test.py --mode malformed_data --workers 10
```

**Проверить:**
- [ ] Сервер корректно отклоняет неверную авторизацию
- [ ] Нет паники при некорректных данных
- [ ] Соединения корректно закрываются

### Этап 3: Проверка утечек
```bash
# Медленные клиенты
python3 server_stress_test.py --mode slowloris --workers 30

# Быстрые переподключения
python3 server_stress_test.py --mode rapid_reconnect --iterations 200
```

**Проверить:**
- [ ] Нет роста памяти (смотреть `ps aux`)
- [ ] Нет накопления соединений (`netstat -an | grep 10443 | wc -l`)
- [ ] SessionManager корректно делает cleanup

### Этап 4: Admin API
```bash
python3 server_stress_test.py --mode admin_api --workers 10
```

**Проверить:**
- [ ] Нет SQL injection
- [ ] Нет path traversal
- [ ] API возвращает корректные коды ошибок

### Этап 5: Полный тест
```bash
# Запускаем все тесты
python3 server_stress_test.py --mode all --workers 15 --iterations 50
```

**После теста проверить:**
```bash
# Проверка процесса
ps aux | grep revsocks-server

# Проверка соединений
netstat -an | grep 10443

# Проверка логов
tail -f /path/to/revsocks.log

# Проверка Admin API
curl http://127.0.0.1:8081/api/sessions
curl http://127.0.0.1:8081/api/agents

# Подключение валидным агентом
./revsocks-agent_v3 -connect wss://127.0.0.1:10443 -pass test123
```

## 🔍 Интерпретация результатов

### ✅ Хорошие признаки
- Success rate > 95% для всех тестов
- Нет паники в логах сервера
- Память стабильна после теста
- Admin API отвечает корректно
- Валидный агент подключается без проблем

### ⚠️ Плохие признаки
- Success rate < 80%
- Паники/ошибки в логах
- Рост памяти во время теста
- Накопление соединений (не закрываются)
- Admin API не отвечает
- Валидный агент не может подключиться после теста

### 🐛 Что искать в логах

**Паники:**
```
panic: runtime error: invalid memory address
panic: concurrent map writes
panic: send on closed channel
```

**Утечки:**
```
goroutine leak detected
too many open files
connection refused (после теста)
```

**Logic errors:**
```
session not found (но агент подключен)
duplicate listener for agent
failed to cleanup session
```

## 💡 Рекомендации

### Перед тестированием
1. Используйте **тестовое окружение**, не прод!
2. Сделайте бэкап конфигурации
3. Запустите сервер с подробным логированием
4. Подготовьте мониторинг (htop, netstat)

### Во время тестирования
1. Следите за логами в реальном времени (`tail -f`)
2. Мониторьте память (`watch -n 1 'ps aux | grep revsocks'`)
3. Проверяйте соединения (`watch -n 1 'netstat -an | grep 10443 | wc -l'`)

### После тестирования
1. Проверьте логи на ошибки/паники
2. Убедитесь что память освободилась
3. Проверьте что соединения закрылись
4. Подключитесь валидным агентом
5. Проверьте Admin API

## 🚨 Troubleshooting

**"Connection refused" при подключении**
- Проверьте что сервер запущен
- Проверьте firewall (`ufw status`)
- Проверьте что используете правильный протокол (ws/wss)

**Тесты висят/не завершаются**
- Ctrl+C для остановки
- Проверьте таймауты в коде (может быть слишком большие)
- Проверьте что сервер отвечает

**"Too many open files"**
- Увеличьте лимит: `ulimit -n 65535`
- Перезапустите тест с меньшим `--workers`

**Сервер упал во время теста**
- **ЭТО БАГ!** Проверьте логи
- Сохраните core dump если есть
- Попробуйте воспроизвести с конкретным режимом

## 📊 Benchmark результаты

Типичные результаты на хорошем сервере:

```
Auth Flood:        100% success, ~200ms avg
Invalid Auth:      100% rejected, ~50ms avg
Malformed Data:    100% handled, ~30ms avg
Slowloris:         100% timeout after 30s
Rapid Reconnect:   100% success, ~100ms avg
Admin API:         All attacks blocked/handled correctly
```

Если ваши результаты значительно хуже - возможно есть проблемы!
